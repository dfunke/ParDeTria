# CMake script for src directory

# build datastructures
add_subdirectory(datastructures)

# build utils
add_subdirectory(utils)

# build triangulation libs
add_subdirectory(lib)

# build executables
add_subdirectory(exe)